**实验报告**

1.
    - 没有
    - 如果没有暂存，每次保存都要提交，会导致版本过多，存储量大，找到某一版本的效率低下。暂存可以减少这方面的问题。
    - `git branch`是显示本地分支，`git branch -a`是显示所有分支。

2. https://github.com/207z/TestLab
3.  
    - **$commit$ $message$规范**：这一博客讲的是关于$commit$ $message$和$change$ $log$的使用规范，详细说明了$commit$ $message$的一种常用书写格式——由$header$, $body$和$footer$三部分组成，可以提供较多历史信息。同时博客还提供了编写$commit$ $message$和生成$change$ $log$的工具。
    - **$git$ $flow$分支控制**：这一博客讲的是关于$git$操作的分支管理，对分支进行了分类，分为了$master$, $develop$, $release$, $feature$, $hotfix$五种分支，并对每种分支的功能作了详细说明。
    - 在项目开发中，常常要面临多人合作开发的情况。如果交流和协作出现了问题，项目就会进入卡壳的状态。$git$便是解决这个问题的有力工具，从这几篇博客就能看出。$commit$ $message$规范规定了协作者们同一个说明规范，$git$ $flow$分支控制，则在操作上让每次更新变成一张清晰的时间图。我们学习$git$，不仅是为了让查看历史版本更加便捷，更是在学习开发者们的合作思维。
4. **实验步骤**：
   - 先使用命令`git checkout -b feature_branch`创建一个$feature$分支。
   - 在$feature$分支中写入新功能，并$commit$。
   - 使用命令`git checkout main`和`git merge --no-ff feature_branch`将新功能合并回$main$分支，并上传至远程仓库。
    